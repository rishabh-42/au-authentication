const ROLES = {
    ADMIN: 'admin',
    STUDENT: 'student',
};

const MIN_PASSWORD_LENGTH = 6;
const MAX_POST_LENGTH = 255;

const constants = {
    ROLES,
    MIN_PASSWORD_LENGTH,
    MAX_POST_LENGTH,
};

module.exports = constants;